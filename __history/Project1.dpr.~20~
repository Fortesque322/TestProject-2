program Project1;

{$APPTYPE CONSOLE}

{$R *.res}

uses
  System.SysUtils,
  Unit1 in 'Unit1.pas';

  var
  CallMetod : TWork;
  str:string;

begin
  try
    CallMetod.SetName(str);
    CallMetod.GetName(Name);
    readln;
    
  except
    on E: Exception do
      Writeln(E.ClassName, 'Erro!', E.Message);
  end;
end.
